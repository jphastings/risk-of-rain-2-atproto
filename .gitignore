# .NET build output
bin/
obj/

# Local-only build config + restored packages
mod/local.props
packages/

# Game assemblies copied in for API verification (not redistributable)
refs/
*.zip

# Runtime artefacts written next to the plugin
config.json
outbox/

# OS
.DS_Store
